import React from "react";
import ReactDOM from "react-dom/client";

const App = () => <h1>Welcome to Noor's React App on Amplify!</h1>;

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
